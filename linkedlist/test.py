from UnorderedList import UnorderedList, ListNode

if __name__ == "__main__":
    node = ListNode(value=123)
    unordered_list = UnorderedList()
    unordered_list.add(123)
    unordered_list.add(777)
    unordered_list.add(15)
    unordered_list.add(2)
    unordered_list.search(17)